package com.petralib.test.manual;

import com.petralib.test.annotation.ManualTest;
import org.junit.jupiter.api.Test;

/**
 * Пример ручного теста.
 * 
 * Ручные тесты требуют визуальной проверки или сложной настройки окружения.
 * Они не запускаются автоматически при выполнении ./gradlew test
 * 
 * Для запуска ручных тестов используйте:
 * ./gradlew testManual
 * 
 * Или для запуска всех тестов (включая ручные):
 * ./gradlew testAll
 */
@ManualTest
class ManualTestExample {

    /**
     * Пример ручного теста, который требует визуальной проверки UI.
     * 
     * ВАЖНО: Этот тест требует ручной проверки результатов!
     * После запуска проверьте вручную:
     * 1. Откройте браузер на http://localhost:8080
     * 2. Проверьте, что все элементы отображаются корректно
     * 3. Проверьте, что стили применяются правильно
     */
    @Test
    void testUIRendering() {
        // TODO: Реализовать тест с реальным браузером (Selenium, Playwright)
        // Пока это заглушка для демонстрации структуры
        
        System.out.println("⚠️  РУЧНОЙ ТЕСТ: Проверьте UI вручную");
        System.out.println("   1. Откройте http://localhost:8080");
        System.out.println("   2. Проверьте отображение всех элементов");
        System.out.println("   3. Проверьте стили и анимации");
        
        // В реальном тесте здесь был бы код для открытия браузера
        // и проверки элементов, но результат все равно требует визуальной проверки
    }

    /**
     * Пример теста производительности, который требует ручного анализа результатов.
     */
    @Test
    void testPerformance() {
        // TODO: Реализовать тест производительности
        // Результаты требуют ручного анализа графиков и метрик
        
        System.out.println("⚠️  РУЧНОЙ ТЕСТ: Производительность");
        System.out.println("   1. Запустите нагрузочное тестирование");
        System.out.println("   2. Проанализируйте графики производительности");
        System.out.println("   3. Проверьте метрики памяти и CPU");
    }

    /**
     * Пример теста интеграции с внешним сервисом, который требует реального подключения.
     */
    @Test
    void testExternalServiceIntegration() {
        // TODO: Реализовать тест с реальным внешним сервисом
        // Требует настройки реального окружения и проверки результатов вручную
        
        System.out.println("⚠️  РУЧНОЙ ТЕСТ: Интеграция с внешним сервисом");
        System.out.println("   1. Убедитесь, что внешний сервис доступен");
        System.out.println("   2. Проверьте логи взаимодействия");
        System.out.println("   3. Проверьте корректность данных в БД");
    }
}

