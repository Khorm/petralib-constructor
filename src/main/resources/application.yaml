jwt:
  header: Authorization
  secret: petra
  expiration: 60000000


spring:
  datasource:
    url: jdbc:postgresql://192.168.0.88/petri_constructor?useUnicode=yes&characterEncoding=UTF8
    username:
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
      format_sql: true



logging:
  level:
    org:
      springframework:
        security: DEBUG



server:
  servlet:
    session:
      cookie:
        path: /
