# Инструкция по запуску тестов TestSprite

## Шаг 1: Запуск приложения

Приложение должно быть запущено на порту 8080 перед выполнением тестов.

### Вариант A: С тестовой H2 базой данных (рекомендуется для тестирования)

```powershell
.\gradlew.bat bootRun --args='--spring.profiles.active=test'
```

### Вариант B: С PostgreSQL (если база доступна)

```powershell
$env:dbUsername="your_username"
$env:dbPassword="your_password"
.\gradlew.bat bootRun
```

Подождите, пока увидите сообщение типа:
```
Started ConstructorApplication in X seconds
```

## Шаг 2: Проверка доступности приложения

В новом терминале проверьте:

```powershell
Test-NetConnection -ComputerName localhost -Port 8080 -InformationLevel Quiet
```

Должно вернуть `True`.

## Шаг 3: Запуск тестов TestSprite

После того как приложение запущено, выполните:

```powershell
node C:\Users\user\AppData\Local\npm-cache\_npx\8ddf6bea01b2519d\node_modules\@testsprite\testsprite-mcp\dist\index.js generateCodeAndExecute
```

## Шаг 4: Просмотр результатов

После выполнения тестов результаты будут в:
- `testsprite_tests/tmp/raw_report.md` - сырой отчет
- `testsprite_tests/testsprite-mcp-test-report.md` - финальный отчет (после обработки)

## Что уже настроено:

✅ `code_summary.json` - описание всех API endpoints
✅ `standard_prd.json` - стандартизированный PRD
✅ `testsprite_backend_test_plan.json` - план тестирования с 10 тест-кейсами
✅ Тестовый профиль с H2 базой данных
✅ Зависимость H2 добавлена в build.gradle

## Тест-кейсы, которые будут выполнены:

1. TC001 - Authentication API: User Login
2. TC002 - Authentication API: User Logout
3. TC003 - Project Management API: Get All Projects
4. TC004 - Project Management API: Create New Project
5. TC005 - Project Management API: Delete Project
6. TC006 - Block Management API: Get Workflow Blocks Page
7. TC007 - Block Management API: Create/Update Action Block
8. TC008 - Type Management API: Create/Update Type
9. TC009 - Scenario Management API: Save Scenario
10. TC010 - Service Management API: Download Service File

